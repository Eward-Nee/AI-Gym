{
  "version": "0.6.4",
  "name": "AI-Gym",
  "notes": "The update check now runs every time the app is opened - on boot, on returning to the foreground, and on regaining a network - and opens the dialog itself. Later means a day, not forever. The daily keep-alive follows the same triggers, so a project cannot be missed because the app was offline at launch or never reloaded."
}
